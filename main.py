import os  # to interacte with the operating system (walking through directories to parse the YAML files)
import re  # for regular expressions - not used right now
import sys  # for command-line arguments


def load_db(directory):
    discog = {}
    file_path = os.path.join()
    with open(file_path, 'r') as file:
        data = file.readlines(file_path)
            if x is x:



    print(data)
    return data


def print_hi(name):
    # Use a breakpoint in the code line below to debug your script.
    print(f'Hi, {name}')  # Press ⌘F8 to toggle the breakpoint.


# Press the green button in the gutter to run the script.
if __name__ == '__main__':
    print_hi('PyCharm')
    directory = "/Users/Study/Downloads/" # default
    load_db(directory)
